//const dfMessenger = document.querySelector('df-messenger');
window.addEventListener('dfMessengerLoaded', function (event) {
/*     // Handle event
    console.log("ESTAMOS EJECUTANDO EN LA PAGINA")
    //RENDER TEXT
    const dfMessenger = document.querySelector('df-messenger');
    dfMessenger.renderCustomText('Bienvenido a ema TESTER');
    dfMessenger.addEventListener('df-list-element-clicked', function (event) {
        console.log("Lista elemento clickeado" + event.detail.element.title);
        alert("pulsaste opcion: "+ event.detail.element.title)

    });
    dfMessenger.addEventListener('df-user-input-entered', function (event) {
        console.log("Lista elemento clickeado" + event.detail.input);
        alert("pulsaste opcion: "+ event.detail.input)

    }); */
});